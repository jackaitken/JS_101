// What will the following two lines of code output?

console.log(0.3 + 0.6);
console.log(0.3 + 0.6 === 0.9);

/*
I believe this will output, 0.9 and then true. Unless we're dealing with
floating point imprecision in which case the first will be some decimal
number close to 0.9 and the second will evaluate to false.

Hey! I was kind of right.
*/